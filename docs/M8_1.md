# M8.1 — Automated qualification

Status: **PASS**

Qualified source commit: `d1fc64dc656ab084b49705934333f1bb2ac620db`

GitHub Actions run: `34778288169`
Job: `103780406050`

## Gates

### Static checks

PASS. `make check` completed with `M7 static checks: PASS`.

### Native Bebbo build

PASS using pinned toolchain image:

`amigadev/m68k-amigaos-gcc@sha256:b18080e6ffca8f793e0f539536a9138e9d2a548ca1a301c7483f43ee15fedfed`

The output was recognized as:

`AmigaOS loadseg()ble executable/binary`

AmBNC SHA-256:

`0418f74f093ceadd6739b6a8b83ae0dbecddad17da50c3ef2fdf2c9ef5ac05f3`

### FS-UAE / AROS guest execution

PASS.

- Model: A1200
- Kickstart: FS-UAE internal AROS ROM
- Guest banner: `AmBNC 0.7.0-m7`
- ARexx reservation banner observed: `AMBNC`
- Legacy CLI usage observed
- `-c CONFIG` multi-network CLI usage observed
- FS-UAE exit 124 is expected because the harness terminates the emulator after collecting guest evidence

Observation: `guest_executed_native_ambnc`.

## Evidence artifact

Artifact name: `fs-uae-aros-qualification`
Artifact ID: `10324073987`
Artifact ZIP SHA-256: `3ea16b1eff459a2b0e0db143ca935b911319f2fdadf10f28ee49cd825a6fb58f`

The artifact includes the AmBNC binary, binary checksum, native build result, pinned toolchain identity, AROS source metadata, guest result, FS-UAE log/version and generated guest configuration.

## Qualification boundary

M8.1 proves that current AmBNC source passes static gates, compiles as a native 68000 AmigaOS executable with the pinned Bebbo toolchain, and loads/starts inside the automated AROS/FS-UAE guest.

It does **not** qualify live IRC connectivity, downstream attachment, reconnect behavior, backlog replay, ARexx commands/hooks, multi-network operation, CAP/SASL behavior or TLS proxy operation. Those remain for M8.2 local AmigaOS runtime qualification.
